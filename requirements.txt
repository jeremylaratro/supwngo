# SupwnGo - Automated Binary Exploitation Framework
#
# pyproject.toml's [project.dependencies] is the canonical dependency list;
# this file mirrors it for `pip install -r requirements.txt` workflows and is
# what setup.py's install_requires reads. Keep the two in sync.

# Core exploitation framework
pwntools>=4.11.0

# Symbolic execution
angr>=9.2.0
angrop>=9.2.0
claripy>=9.2.0

# Binary parsing and analysis
pyelftools>=0.29
capstone>=5.0.0
keystone-engine>=0.9.2
unicorn>=2.0.0
lief>=0.13.0

# ROP gadget finding
ropper>=1.13.0

# CLI and UI
click>=8.1.0
rich>=13.0.0

# Database
sqlalchemy>=2.0.0

# Network requests (for libc database)
requests>=2.28.0

# Strategy graph / config parsing
networkx>=3.0
pyyaml>=6.0

# Optional extras (install via `pip install -e ".[ai,z3,r2,dev]"`):
#   ai  -> anthropic, openai   (supwngo/ai/*, lazily imported, guarded)
#   z3  -> z3-solver           (exploit/rop/z3_solver.py, lazily imported)
#   r2  -> r2pipe              (radare2 integration, optional)
#   dev -> pytest, black, mypy, etc.
